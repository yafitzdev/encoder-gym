{
  "name": "@encoder-gym/ui",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "description": "Encoder Gym local encoder and candidate comparison workspace.",
  "type": "module",
  "main": "dist/bootstrap.cjs",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "build:backend": "cargo build --manifest-path ../Cargo.toml --package synthetic-data-cli --bin synth --locked",
    "build:test-fixtures": "cargo build --manifest-path ../Cargo.toml --package synthetic-data-cli --features test-fixtures --bin synth-benchmark-fixture --locked",
    "verify:current": "npm run build:backend && npm run build && electron . --verify-managed-current",
    "build": "node scripts/build.mjs",
    "start": "npm run build:backend && npm run build && electron .",
    "smoke": "npm run build:backend && npm run build:test-fixtures && npm run build && node scripts/smoke.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "verify:overview": "npm run typecheck && npm run build && electron scripts/verify-overview.cjs",
    "check": "npm run build:backend && npm run typecheck && npm run build && npm test",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "26.4.0",
    "electron": "43.2.0",
    "esbuild": "^0.25.0",
    "typescript": "7.0.2"
  }
}
